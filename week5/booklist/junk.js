/* books.forEach((book) => {
        let bookProfile = document.createElement("div")
        let bookTitle = document.createElement("p")
        bookTitle.innerText = "book.title"
        let bookAuthor = document.createElement("p")
        bookProfile.append(bookTitle)
        bookProfile.append(bookAuthor)

        
    }) */